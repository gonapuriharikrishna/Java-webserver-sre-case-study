apiVersion: v1
kind: Service
metadata:
  name: {{ include "java-webserver-charts.fullname" . }}
  labels:
    {{- include "java-webserver-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "java-webserver-charts.selectorLabels" . | nindent 4 }}
